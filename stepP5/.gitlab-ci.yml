before_script:
  - git submodule update --init --recursive

build-and-run-y2:
  tags: [yuria2]
  script:
    - rebench --experiment="CI ID $CI_PIPELINE_ID" --branch="$CI_COMMIT_REF_NAME" rebench.conf java-paper
  artifacts:
    paths:
      - rebench.data
